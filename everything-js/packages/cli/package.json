{
  "name": "@open-context/cli",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "open-context": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/index.js",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@napi-rs/keyring": "^1.3.0",
    "better-auth": "^1.6.23"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6.0"
  }
}
